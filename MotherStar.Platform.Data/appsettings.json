{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    /*********************************************************
    SeoDb should be set in user settings for local dev
    and is configured using secrets in GitHub for deployments 
    *********************************************************/
    "SeoDb": "",
    /*********************************************************
    SecurityDb should be set in user settings for local dev
    and is configured using secrets in GitHub for deployments 
    *********************************************************/
    "SecurityDb": "",
    /*********************************************************
    Entitlements should be set in user settings for local dev
    and is configured using secrets in GitHub for deployments 
    *********************************************************/
    "EntitlementsDb": "",
    /*********************************************************
    MonitoringDb should be set in user settings for local dev
    and is configured using secrets in GitHub for deployments 
    *********************************************************/
    "MonitoringDb": "",
    /*********************************************************
    SocialMediaDb should be set in user settings for local dev
    and is configured using secrets in GitHub for deployments 
    *********************************************************/
    "SocialMediaDb": ""
  }
}
